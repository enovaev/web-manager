image: node:latest

before_script:
  - apt-get update -qy
  - gem install dpl

stages:
  - production

production:
  type: deploy
  stage: production
  image: node:latest
  script:
    - dpl --provider=heroku --app=$HEROKU_APP_PRODUCTION --api-key=$HEROKU_API_KEY
  only:
    - master